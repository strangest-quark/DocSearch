server {
    listen 80;
    server_name www.teacher-portal.tk teacher-portal.tk;


location / {
        try_files $uri @app;
    }
    location @app {
        include uwsgi_params;
        uwsgi_pass app:5001;
    }
}